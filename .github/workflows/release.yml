name: Build and Release on Tag

on:
  push:
    tags:
      - 'v*'
    branches:
      - main

jobs:
  build-and-release:
    name: Release
    if: github.event.name == 'Release' && github.event.action == 'created'
    permissions: write-all
    runs-on: windows-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.10'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
    
    - name: Build with PyInstaller
      run: |
        pyinstaller -n "Orientador_Vocacional_CUCOSTA" --add-data "resources;resources" --noconsole --onefile  __main__.py
        
      env:
        PYTHONPATH: ${{ env.PYTHONPATH }}

    - name: Copy resources and compress app
      run: |
        xcopy "resources" "dist\resources"
        Compress-Archive dist/ "dist/Orientador Vocacional CUCOSTA" 
    
    - name: Show contents of the folder
      run: |
        dir
        dir dist/

    - name: Create Release
      id: create_release
      uses: actions/create-release@v1
      with:
        tag_name: ${{ github.ref }}
        release_name: Release ${{ github.ref }}
        body: |
          Release ${{ github.ref }}
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

    - name: Upload Release Asset
      id: upload-release-asset
      uses: actions/upload-release-asset@v1
      with:
        upload_url: ${{ steps.create_release.outputs.upload_url }}
        asset_path: dist/Orientador_Vocacional_CUCOSTA.zip 
        asset_name: Orientador_Vocacional_CUCOSTA 
        asset_content_type: application/zip
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
